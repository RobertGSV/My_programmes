#include <stdio.h>


int numbers[5];
int lowest = 0;

int main(void)
{
    int i=0;
    for(i=0; i < 4; i++)
    {
    scanf("%d\n", &numbers[i]);
    if (numbers[i] > lowest)
    {
          lowest = numbers[i];
    }
    }
    printf("The lowest number is %d\n\n", highest);
}
